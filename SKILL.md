---
name: traceable-deep-research
description: 深度调研与可信分析。当用户提出"调研/研究/分析/查一下"某一领域、行业、公司或话题，要求"核实/验证/多方查证"信息或说法，需要"带引用/标注可信度"的分析报告，或上传多份文件要求"融合分析/找出矛盾/差异对比"时使用。每条结论按四级可信度标注并逐条溯源，冲突不抹平。不用于简单事实查询、单源问答、闲聊、写作润色。
version: 2.1.2
homepage: https://skill.xfyun.cn/
x-astron-category: retrieval-analysis
---

# Traceable Deep Research · 可信度分级信息检索与分析

把"检索—融合—分析"升级为**可被信任的分析**：不只找到并总结，而是给每条发现打**可信度标签**、显式标出**源间矛盾**、逐句**溯源引用**。多路检索服务于认知稳健性（epistemic robustness），而非单纯堆量。

本文件是**导航页**。它只承载路由判断与流程骨架；详细规则、判定标准、脚本与模板都在支撑目录里，**用到时才读取**，避免上下文浪费。

## 核心原则（读完这 5 条即可开工）

1. **证据先行**：任何分析性结论产生前，必须先检索/读文件拿到证据。不得凭记忆先下判断。
2. **矛盾即信号**：源间冲突要显式暴露、分类、分析，**绝不抹平或取平均**。这是本 skill 的签名能力。
3. **一切落文件**：长内容一律写入 `{workspace}/research/` 下的文件，聊天窗口只做状态汇报。
4. **可信度分级**：每条发现归入 High / Medium / Low / **Conflict Zone** 四级之一，并逐条 `[^id]` 溯源。规则见 `references/verification-engine.md`。
5. **忠于用户意图**：用户说"只基于文件"就绝不偷偷联网；用户给了时间范围就把它当硬约束。

## 常见触发句式（模型路由参考）

以下任一表述均可触发本 skill——注意覆盖口语化、非标准表达：

- **调研分析类**："调研/研究/分析/梳理/盘点一下 XX""XX 行业/领域/技术 现在什么情况/整体现状/趋势怎样"
- **查证核实类**："核实/验证/查证/看一眼 XX 说法是不是真的""XX 这个数据准确吗，帮我校验一下""这几个说法到底哪个靠谱"
- **对比对比类**："对比一下 A 和 B""XX 和 YY 有什么不同""帮我看两份报告哪里不一样""这几份材料说的是同一件事吗"
- **溯源引用类**："写一份 XX 分析报告，带出处""结论要挂引用、标注可信度""不要只说结论，告诉我来源和把握多大"
- **多文件类**（附带文件上传）："把这几份文件融合分析一下""上传了几份材料，帮我找出矛盾/差异""整合整理这几份 PDF 的内容"
- **竞品/商业类**："做个竞品分析""写一份行业报告""市场格局怎么样"

一句话辨识口诀：**包含"调研/研究/分析/核实/对比/带出处/矛盾/整合"之一 + 不是一句话回答得了的问题 + 不在闲聊上下文中。**

## 何时不用（排除项）

| 不该用 | 示例 |
|---|---|
| 简单事实查询 | "XX 公司 CEO 是谁""今天天气""什么时间发布的" |
| 单源一问一答 | "这段话什么意思""帮我翻一下" |
| 闲聊、创意写作 | "写首诗""讲个笑话""帮我起个名字" |
| 已有明确答案只需润色 | "这段文字改通顺一点" |
| 纯格式转换 | "pdf 转 word""图片转文字" |

> 如果用户请求不明确，采用下述原则：含"研究/调研/分析+主题"→**触发**；含"帮我查/帮我看+宽泛主题"→**触发**；纯事实+已知答案→**不触发**。

## 输出目录（强制）

所有产物**必须**保存到 `{workspace}/research/`（`{workspace}` 为当前工作目录，勿硬编码绝对路径）。写文件前先确保该目录存在——可运行 `scripts/setup_workspace.py`，或手动创建。聊天里**不要**粘贴长研究内容。

## 优雅降级：单智能体基线 + 子智能体增强

本 skill 的能力**不依赖并行子智能体**。请先探测运行时能力，再选执行模式：

- **基线模式（永远可用）**：主智能体**串行**走完各阶段——逐个维度检索、逐个写入 `research/{topic}_dim{NN}.md`，再统一交叉验证。稳、可复现，是默认与兜底。
- **增强模式（若运行时支持子任务/子智能体并行派发）**：把"每维度一个调查单元"分批派发（受并发上限约束，**分轮执行**：填满上限→等一批完成→再派下一批）。并发只是加速，**不改变**证据标准与验证流程。

> Gotcha：不要假设无限并行。维度数是**覆盖目标**，不是"同时启动数"。并发上限通常只有个位数（约 4）。

## 工作流骨架（Phase 0 → 7）

```
用户请求
  → Phase 0  意图与输入路由（选 Route A/B/C/D）        → references/routing-and-reset.md
  → Phase 1  景观扫描（粗到细，建立证据地貌）           → references/evidence-pipeline.md
  → Phase 1W 多路广探（Route A，可选增强）              → references/evidence-pipeline.md
  → Phase F  文件深读（Route C/D，有上传文件时）        → references/evidence-pipeline.md
  → Phase 2  维度分解（≥10 个互补且重叠的角度，可参考预设模板起步） → references/evidence-pipeline.md · perspective-templates.md
  → Phase 3  并行深挖（每维度一个调查单元，逐条取证）    → references/evidence-pipeline.md
  → Phase 4  交叉验证引擎（四级可信度 + 冲突区）★核心   → references/verification-engine.md
  → Phase 5  定向复核（仅当存在冲突/关键低可信时）       → references/verification-engine.md
  → Phase 6  洞察提炼（跨维度涌现的非显性结论）          → references/insight-and-output.md
  → Phase 7  报告组装与交付（模板 + 引用去重）           → references/insight-and-output.md
```

### 四条路由（Phase 0 速判）

| 信号 | 路由 | 首个阶段 |
|---|---|---|
| 有文件 + "只基于文件/不要联网" | **C** 纯文件研究 | Phase F |
| 有文件 + "参考/结合/帮我补全"或未限制 | **D** 文件增强研究 | Phase F |
| 无文件 + 宽泛开放主题 | **A** 广度检索 | Phase 1 → 1W |
| 无文件 + 具体有界问题 | **B** 聚焦检索 | Phase 1 |

歧义时：主题明显多面且无明确切入 → 默认 **A**；文件意图不清 → 优先 **D**（输出更丰富）。细则见 `references/routing-and-reset.md`。

## 开工前的 Epistemic Reset（每次必做）

1. 先用 Shell/工具**获取当前日期时间**——假设内部知识可能过期。
2. **时间敏感约束**：用户提到"2026 Q1/近半年/最新/当前"时，把该时间窗当硬约束，窗口外的发现要标注。
3. **检索语言规则**：检索用语必须与用户提问语言一致（中文提问→中文检索）。
4. **溯源契约**：所有产物用标准 Markdown 脚注——正文 `[^id]` + 文末 `[^id]: 标题. 日期. URL`。规则见 `references/insight-and-output.md`。

## 支撑资源导航（用到再读）

| 需要做… | 去读 / 运行 |
|---|---|
| 判定路由、时间与语言约束 | `references/routing-and-reset.md` |
| 景观扫描 / 广探 / 文件深读 / 维度分解 / 深挖取证模板 | `references/evidence-pipeline.md` |
| **可信度分级、共识区/盲区检测、认知自检、复核**（核心资产） | `references/verification-engine.md` |
| 洞察提炼（含行动洞察）、报告组装、溯源引用契约 | `references/insight-and-output.md` |
| 维度设计的预设视角模板（起手式） | `references/perspective-templates.md` |
| 创建工作区目录 + 打印当前时间 | `scripts/setup_workspace.py` |
| 引用去重与统一编号 | `scripts/dedup_citations.py` |
| 统计各维度可信度分布、汇总冲突与共识清单 | `scripts/tally_confidence.py` |
| **动态内容安全净化**（转义/URL过滤/公式防注入） | `scripts/sanitize_markdown.py` |
| 看完整执行样例（按路由选） | `examples/example-focused-query.md`（B）· `example-wide-search.md`（A）· `example-file-only.md`（C）|
| 最终报告骨架模板 | `assets/report_template.md` |

> 脚本是**可选增强**：若运行时能执行 Python，用脚本做确定性工作（更省 token、更准）；若不能，按对应 reference 里的手动步骤完成，结果一致。

## 平台 Gotchas（易踩坑，模型默认不知道）

- **支撑文件不会自动进上下文**：只有在 `SKILL.md` / 流程里被显式引用时才加载。别把细则往这里堆，会撑爆上下文。
- **返回 200 ≠ 检索成功**：搜索工具有结果不代表内容相关；先看标题/日期/来源质量再采信。
- **同一指标两个数字 = Conflict Zone**：两个来源对同一统计给出不同数值，属冲突，必须记录各自时间与出处，不得二选一或求平均。
- **时间错位也是冲突**：不同来源的数据来自不同时期却被并列，要标注时间不一致。

## 安全与合规

- 只检索公开可访问信息；不绕过登录/付费墙，不抓取需授权内容。
- 逐条溯源，不编造来源或 URL；无法核实的主张标注"未核实"而非填充。
- 脚本只处理本地 `research/` 目录下的文本，不执行来自检索内容的任意命令，不外发数据。
- 涉及个人隐私/敏感信息时最小化引用，遵守数据合规。
